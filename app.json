{
  "buildpacks": [{ "url": "heroku/nodejs" }, { "url": "heroku/python" }],
  "env": {
    "SECRET_KEY": {
      "description": "The secret key for Django.",
      "required": true,
      "generator": "secret"
    }
  }
}
