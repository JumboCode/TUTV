import React from 'react';
import ViewButton from './ViewButton';
import ProjectBox from './ProjectBox';

const PastRequests: React.FC = () => {
  return (
      <React.Fragment>
          <ViewButton />
          <ProjectBox />
      </React.Fragment>
};

export default Button;